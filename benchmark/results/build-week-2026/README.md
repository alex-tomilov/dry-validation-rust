# Build Week 2026 benchmark evidence

Status: **full evidence not generated yet**.

This directory reserves the reviewed location for the clean-commit, five-run
benchmark package. No performance number is claimed from this placeholder.
Run [`reproduce.sh`](reproduce.sh) on an idle Linux x86-64 host after installing
the pinned benchmark dependencies. A successful run replaces this placeholder
with environment metadata, every raw worker result, and mechanically derived
JSON and Markdown summaries.

Quick-mode output belongs under `tmp/` and is smoke evidence only.
