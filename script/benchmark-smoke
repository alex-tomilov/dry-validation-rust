#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

N="${N:-1000}" \
WARMUP="${WARMUP:-100}" \
ENGINE="${ENGINE:-rust}" \
FORMAT="${FORMAT:-json}" \
ruby -Ilib benchmark/schema_throughput.rb
