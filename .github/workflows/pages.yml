name: Deploy documentation

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Setup mdBook
        uses: peaceiris/actions-mdbook@v2
        with:
          mdbook-version: latest

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Build documentation site
        run: mdbook build --dest-dir _site

      - name: Build YARD API reference
        run: bundle exec yard doc

      - name: Add YARD API reference to site
        run: mkdir -p _site/yard && cp -r doc/* _site/yard/

      - name: Build Rust API reference
        run: cargo doc --manifest-path ext/dry_validation_rust/Cargo.toml --no-deps

      - name: Add Rust API reference to site
        run: mkdir -p _site/rustdoc && cp -r target/doc/* _site/rustdoc/

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
